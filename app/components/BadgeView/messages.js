/*
 * BadgeView Messages
 *
 * This contains all the text for the BadgeView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BadgeView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BadgeView component!',
  },
});
