/*
 * HeaderView Messages
 *
 * This contains all the text for the HeaderView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderView';

export default defineMessages({
  appName: {
    id: `${scope}.appName`,
    defaultMessage: 'QUéPASA',
  },
});
