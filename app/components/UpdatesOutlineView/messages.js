/*
 * UpdatesOutlineView Messages
 *
 * This contains all the text for the UpdatesOutlineView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UpdatesOutlineView';

export default defineMessages({
  newUpdatesLabel: {
    id: `${scope}.updates-label`,
    defaultMessage: 'Epic things happened this week',
  },
  totalTeamsLabel: {
    id: `${scope}.updates-label`,
    defaultMessage: 'Teams improved something this week',
  },
});
