/*
 * UpdateMetricView Messages
 *
 * This contains all the text for the UpdateMetricView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UpdateMetricView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpdateMetricView component!',
  },
});
