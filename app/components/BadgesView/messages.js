/*
 * BadgesView Messages
 *
 * This contains all the text for the BadgesView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BadgesView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BadgesView component!',
  },
});
